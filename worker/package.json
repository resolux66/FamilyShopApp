{
  "name": "familycart-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute familycart-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute familycart-db --local --file=./schema.sql"
  },
  "dependencies": {
    "esbuild": "^0.27.3",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.72.0"
  }
}
